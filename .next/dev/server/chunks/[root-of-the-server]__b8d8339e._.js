module.exports = [
"[externals]/next/dist/compiled/next-server/app-route-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-route-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/next-server/app-page-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-page-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-unit-async-storage.external.js [external] (next/dist/server/app-render/work-unit-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-unit-async-storage.external.js", () => require("next/dist/server/app-render/work-unit-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-async-storage.external.js [external] (next/dist/server/app-render/work-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-async-storage.external.js", () => require("next/dist/server/app-render/work-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/shared/lib/no-fallback-error.external.js [external] (next/dist/shared/lib/no-fallback-error.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/shared/lib/no-fallback-error.external.js", () => require("next/dist/shared/lib/no-fallback-error.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/after-task-async-storage.external.js [external] (next/dist/server/app-render/after-task-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/after-task-async-storage.external.js", () => require("next/dist/server/app-render/after-task-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/child_process [external] (child_process, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("child_process", () => require("child_process"));

module.exports = mod;
}),
"[externals]/fs [external] (fs, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("fs", () => require("fs"));

module.exports = mod;
}),
"[externals]/https [external] (https, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("https", () => require("https"));

module.exports = mod;
}),
"[externals]/stream [external] (stream, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("stream", () => require("stream"));

module.exports = mod;
}),
"[externals]/os [external] (os, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("os", () => require("os"));

module.exports = mod;
}),
"[externals]/events [external] (events, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("events", () => require("events"));

module.exports = mod;
}),
"[externals]/process [external] (process, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("process", () => require("process"));

module.exports = mod;
}),
"[externals]/util [external] (util, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("util", () => require("util"));

module.exports = mod;
}),
"[externals]/path [external] (path, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("path", () => require("path"));

module.exports = mod;
}),
"[externals]/crypto [external] (crypto, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("crypto", () => require("crypto"));

module.exports = mod;
}),
"[externals]/querystring [external] (querystring, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("querystring", () => require("querystring"));

module.exports = mod;
}),
"[externals]/buffer [external] (buffer, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("buffer", () => require("buffer"));

module.exports = mod;
}),
"[externals]/http2 [external] (http2, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("http2", () => require("http2"));

module.exports = mod;
}),
"[externals]/zlib [external] (zlib, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("zlib", () => require("zlib"));

module.exports = mod;
}),
"[externals]/url [external] (url, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("url", () => require("url"));

module.exports = mod;
}),
"[project]/src/lib/google-calendar.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "CalendarError",
    ()=>CalendarError,
    "createCalendarEvent",
    ()=>createCalendarEvent,
    "getAvailableSlots",
    ()=>getAvailableSlots,
    "getCalendarClient",
    ()=>getCalendarClient,
    "getCalendarId",
    ()=>getCalendarId,
    "validateAuthentication",
    ()=>validateAuthentication,
    "withErrorHandling",
    ()=>withErrorHandling
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$googleapis$2f$build$2f$src$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/googleapis/build/src/index.js [app-route] (ecmascript)");
;
function getCalendarClient() {
    // Validate required environment variables
    if (!process.env.GOOGLE_SERVICE_ACCOUNT_EMAIL) {
        throw new Error("Missing GOOGLE_SERVICE_ACCOUNT_EMAIL environment variable");
    }
    if (!process.env.GOOGLE_PRIVATE_KEY) {
        throw new Error("Missing GOOGLE_PRIVATE_KEY environment variable");
    }
    if (!process.env.GOOGLE_CALENDAR_ID) {
        throw new Error("Missing GOOGLE_CALENDAR_ID environment variable");
    }
    // Create JWT auth client with service account credentials
    const auth = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$googleapis$2f$build$2f$src$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["google"].auth.JWT({
        email: process.env.GOOGLE_SERVICE_ACCOUNT_EMAIL,
        key: process.env.GOOGLE_PRIVATE_KEY.replace(/\\n/g, "\n"),
        scopes: [
            "https://www.googleapis.com/auth/calendar",
            "https://www.googleapis.com/auth/calendar.events"
        ]
    });
    // Return authenticated calendar client
    return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$googleapis$2f$build$2f$src$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["google"].calendar({
        version: "v3",
        auth
    });
}
function getCalendarId() {
    const calendarId = process.env.GOOGLE_CALENDAR_ID;
    if (!calendarId) {
        throw new Error("Missing GOOGLE_CALENDAR_ID environment variable");
    }
    return calendarId;
}
class CalendarError extends Error {
    code;
    originalError;
    constructor(message, code, originalError){
        super(message), this.code = code, this.originalError = originalError;
        this.name = "CalendarError";
    }
    /**
   * Returns a user-friendly error message
   */ getUserMessage() {
        switch(this.code){
            case "AUTH_ERROR":
                return "Unable to authenticate with Google Calendar. Please try again later.";
            case "CALENDAR_NOT_FOUND":
                return "Calendar not found. Please contact support.";
            case "PERMISSION_DENIED":
                return "Permission denied to access calendar. Please contact support.";
            case "NETWORK_ERROR":
                return "Network error. Please check your connection and try again.";
            case "INVALID_REQUEST":
                return "Invalid request. Please check your input and try again.";
            default:
                return "An unexpected error occurred. Please try again later.";
        }
    }
}
async function withErrorHandling(fn) {
    try {
        return await fn();
    } catch (error) {
        // Handle Google API errors
        if (error.code === 401 || error.code === 403) {
            throw new CalendarError("Authentication or permission error", error.code === 401 ? "AUTH_ERROR" : "PERMISSION_DENIED", error);
        }
        if (error.code === 404) {
            throw new CalendarError("Calendar not found", "CALENDAR_NOT_FOUND", error);
        }
        if (error.code === 400) {
            throw new CalendarError("Invalid request", "INVALID_REQUEST", error);
        }
        if (error.code === "ENOTFOUND" || error.code === "ETIMEDOUT") {
            throw new CalendarError("Network error", "NETWORK_ERROR", error);
        }
        // Unknown error
        throw new CalendarError(error.message || "Unknown error", "UNKNOWN_ERROR", error);
    }
}
async function validateAuthentication() {
    return withErrorHandling(async ()=>{
        const calendar = getCalendarClient();
        const calendarId = getCalendarId();
        // Try to fetch calendar metadata to validate authentication
        await calendar.calendars.get({
            calendarId
        });
        return true;
    });
}
/**
 * Business hours configuration
 */ const BUSINESS_HOURS = {
    start: 9,
    end: 18,
    slotDurationMinutes: 30,
    excludeWeekends: true
};
async function getAvailableSlots() {
    return withErrorHandling(async ()=>{
        const calendar = getCalendarClient();
        const calendarId = getCalendarId();
        // Calculate time range: now to 14 days from now
        const now = new Date();
        const endDate = new Date();
        endDate.setDate(endDate.getDate() + 14);
        // Query freebusy information
        const response = await calendar.freebusy.query({
            requestBody: {
                timeMin: now.toISOString(),
                timeMax: endDate.toISOString(),
                items: [
                    {
                        id: calendarId
                    }
                ],
                timeZone: "Africa/Lagos"
            }
        });
        // Extract busy periods
        const busyPeriods = response.data.calendars?.[calendarId]?.busy || [];
        // Generate all possible time slots within business hours
        const allSlots = generateBusinessHourSlots(now, endDate);
        // Filter out busy slots
        const availableSlots = allSlots.filter((slot)=>{
            const slotStart = new Date(slot.start);
            const slotEnd = new Date(slot.end);
            // Check if slot overlaps with any busy period
            const isOverlapping = busyPeriods.some((busy)=>{
                const busyStart = new Date(busy.start);
                const busyEnd = new Date(busy.end);
                // Slot overlaps if it starts before busy ends and ends after busy starts
                return slotStart < busyEnd && slotEnd > busyStart;
            });
            return !isOverlapping;
        });
        return availableSlots;
    });
}
/**
 * Generates all possible time slots within business hours for the given date range
 * 
 * @param startDate - Start of the date range
 * @param endDate - End of the date range
 * @returns Array of time slots
 */ function generateBusinessHourSlots(startDate, endDate) {
    const slots = [];
    const currentDate = new Date(startDate);
    currentDate.setHours(0, 0, 0, 0); // Start at midnight
    while(currentDate <= endDate){
        // Skip weekends if configured
        const dayOfWeek = currentDate.getDay();
        const isWeekend = dayOfWeek === 0 || dayOfWeek === 6; // Sunday = 0, Saturday = 6
        if (!BUSINESS_HOURS.excludeWeekends || !isWeekend) {
            // Generate slots for this day
            for(let hour = BUSINESS_HOURS.start; hour < BUSINESS_HOURS.end; hour++){
                // Generate slots at 30-minute intervals
                for(let minute = 0; minute < 60; minute += BUSINESS_HOURS.slotDurationMinutes){
                    const slotStart = new Date(currentDate);
                    slotStart.setHours(hour, minute, 0, 0);
                    const slotEnd = new Date(slotStart);
                    slotEnd.setMinutes(slotEnd.getMinutes() + BUSINESS_HOURS.slotDurationMinutes);
                    // Skip if slot is in the past
                    if (slotStart <= new Date()) {
                        continue;
                    }
                    // Skip if slot extends beyond business hours
                    if (slotEnd.getHours() > BUSINESS_HOURS.end) {
                        continue;
                    }
                    slots.push({
                        start: slotStart.toISOString(),
                        end: slotEnd.toISOString(),
                        date: formatDate(slotStart),
                        time: formatTime(slotStart)
                    });
                }
            }
        }
        // Move to next day
        currentDate.setDate(currentDate.getDate() + 1);
    }
    return slots;
}
/**
 * Formats a date as YYYY-MM-DD
 * 
 * @param date - Date to format
 * @returns Formatted date string
 */ function formatDate(date) {
    const year = date.getFullYear();
    const month = String(date.getMonth() + 1).padStart(2, "0");
    const day = String(date.getDate()).padStart(2, "0");
    return `${year}-${month}-${day}`;
}
/**
 * Formats a time in 12-hour format with AM/PM
 * 
 * @param date - Date to format
 * @returns Formatted time string (e.g., "2:00 PM")
 */ function formatTime(date) {
    let hours = date.getHours();
    const minutes = date.getMinutes();
    const ampm = hours >= 12 ? "PM" : "AM";
    hours = hours % 12;
    hours = hours ? hours : 12; // Convert 0 to 12
    const minutesStr = String(minutes).padStart(2, "0");
    return `${hours}:${minutesStr} ${ampm}`;
}
/**
 * Gets the event duration in minutes based on collection type
 * 
 * Requirements: 11.4, 11.5, 11.6, 11.7
 * - Bridal: 60 minutes
 * - Bespoke: 45 minutes
 * - Ready-to-Wear (RTW): 30 minutes
 * 
 * @param collectionType - The type of collection
 * @returns Duration in minutes
 */ function getEventDuration(collectionType) {
    switch(collectionType){
        case "bridal":
            return 60;
        case "bespoke":
            return 45;
        case "rtw":
            return 30;
        default:
            return 30; // Default to 30 minutes
    }
}
/**
 * Formats collection type for display
 * 
 * @param collectionType - The collection type
 * @returns Formatted collection type string
 */ function formatCollectionType(collectionType) {
    switch(collectionType){
        case "bridal":
            return "Bridal";
        case "bespoke":
            return "Bespoke";
        case "rtw":
            return "Ready-to-Wear";
        default:
            return collectionType;
    }
}
async function createCalendarEvent(startTime, customerDetails) {
    return withErrorHandling(async ()=>{
        const calendar = getCalendarClient();
        const calendarId = getCalendarId();
        // Calculate end time based on collection type
        const duration = getEventDuration(customerDetails.collectionType);
        const start = new Date(startTime);
        const end = new Date(start);
        end.setMinutes(end.getMinutes() + duration);
        // Format collection type for display
        const collectionTypeDisplay = formatCollectionType(customerDetails.collectionType);
        // Create event description with customer details
        const description = `
Consultation Booking - ${collectionTypeDisplay}

Customer Details:
- Name: ${customerDetails.name}
- Email: ${customerDetails.email}
- Phone: ${customerDetails.phone}
- Collection Type: ${collectionTypeDisplay}

This is an automated booking from the Vickie's Atelier website.
    `.trim();
        // Create the calendar event
        try {
            const response = await calendar.events.insert({
                calendarId,
                sendUpdates: "all",
                requestBody: {
                    summary: `Consultation: ${customerDetails.name} (${collectionTypeDisplay})`,
                    description,
                    start: {
                        dateTime: start.toISOString(),
                        timeZone: "Africa/Lagos"
                    },
                    end: {
                        dateTime: end.toISOString(),
                        timeZone: "Africa/Lagos"
                    },
                    attendees: [
                        {
                            email: customerDetails.email,
                            displayName: customerDetails.name,
                            responseStatus: "needsAction"
                        }
                    ],
                    reminders: {
                        useDefault: false,
                        overrides: [
                            {
                                method: "email",
                                minutes: 24 * 60
                            },
                            {
                                method: "popup",
                                minutes: 60
                            }
                        ]
                    }
                }
            });
            const event = response.data;
            // Log successful event creation
            console.log(`[Calendar] Event created successfully: ${event.id} for ${customerDetails.name}`);
            return {
                eventId: event.id,
                eventLink: event.htmlLink,
                startTime: event.start.dateTime,
                endTime: event.end.dateTime
            };
        } catch (error) {
            // Log the error with customer details for debugging
            console.error(`[Calendar] Failed to create event for ${customerDetails.name} (${customerDetails.email}):`, error.message || error);
            // Re-throw as CalendarError for consistent error handling
            throw error;
        }
    });
}
}),
"[project]/src/app/api/calendar/available-slots/route.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "GET",
    ()=>GET
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next/server.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$google$2d$calendar$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/google-calendar.ts [app-route] (ecmascript)");
;
;
async function GET() {
    try {
        // Fetch available slots from Google Calendar
        const slots = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$google$2d$calendar$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getAvailableSlots"])();
        // Group slots by date for easier display
        const slotsByDate = {};
        slots.forEach((slot)=>{
            if (!slotsByDate[slot.date]) {
                slotsByDate[slot.date] = [];
            }
            slotsByDate[slot.date].push(slot);
        });
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            success: true,
            slots: slotsByDate,
            businessHours: {
                start: "9:00 AM",
                end: "6:00 PM",
                timezone: "Africa/Lagos (WAT)"
            }
        });
    } catch (error) {
        // Log detailed error for debugging (server-side only)
        console.error("[Calendar API] Error fetching available slots:", {
            error: error instanceof Error ? error.message : String(error),
            stack: error instanceof Error ? error.stack : undefined,
            timestamp: new Date().toISOString()
        });
        // Handle CalendarError with user-friendly messages
        if (error instanceof __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$google$2d$calendar$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["CalendarError"]) {
            return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                success: false,
                error: error.getUserMessage(),
                code: error.code,
                retryable: true
            }, {
                status: error.code === "AUTH_ERROR" ? 401 : 503
            });
        }
        // Handle unexpected errors with user-friendly message
        // Don't expose technical details to the client
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            success: false,
            error: "We're having trouble loading available time slots. Please try again in a moment.",
            retryable: true
        }, {
            status: 503
        });
    }
}
}),
];

//# sourceMappingURL=%5Broot-of-the-server%5D__b8d8339e._.js.map